/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html, ts}" // ficara olhando esses arquivos que irão receber o estilo do tailwind e ts por que a gente consegue fazer o template
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

